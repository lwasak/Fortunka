<p id="notice" class="green"><b><%= notice %></b></p>

<article class="slot">
<header>
    <%= link_to 'Edit', edit_fortune_path(@fortune),  remote: true, :class => "edit" %>
    <%= link_to 'Destroy', @fortune, confirm: 'Are you sure?', method: :delete, remote: true, :class => "destroy" %>
</header>

<p><b><%= @fortune.title %></b></p>
<p>
  <%= @fortune.body %>
</p>
</article>

<% if @fortune.comments.size > 0 %>
  <h2>Comments</h2>
  <% for comments in @fortune.comments %>
    <h3><%= comments.name %></h3>
    <p><%= comments.content %></p>
  <% end %>
<% end %>

<h2>New Comment</h2>

<%= form_for [@fortune, Comments.new] do |f| %>
  <p><%= f.label :author %> <%= f.text_field :author %></p>
  <p><%= f.text_area :body, :rows => 10 %></p>
  <p><%= f.submit %></p>
<% end %>


<%= link_to 'Back', fortunes_path %>