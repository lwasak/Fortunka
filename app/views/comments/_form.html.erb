<%= simple_form_for(@comment) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :author %>
    <%= f.input :body %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
