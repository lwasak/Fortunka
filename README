Simple post -> comment application on ruby on rails.

The database is on mongoDB.

There is a file called data.json in the main folder, it contains test data to upload to your mongo database.

Use this command:

mongoimport --drop -d lwasak -c fortunes data.json


The application uses gems:

-- formtastic
-- kaminari
-- mongoid
-- sass & bourbon
-- devise
